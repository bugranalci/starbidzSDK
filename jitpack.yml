# JitPack build configuration for Starbidz SDK
# https://github.com/bugranalci/starbidzSDK

jdk:
  - openjdk17

before_install:
  - chmod +x adapters/android/gradlew

install:
  - adapters/android/gradlew -p adapters/android :starbidz:assembleRelease :starbidz:publishToMavenLocal -x test
