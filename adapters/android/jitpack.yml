jdk:
  - openjdk17

before_install:
  - chmod +x gradlew

install:
  - ./gradlew :starbidz:assembleRelease :starbidz:publishToMavenLocal :starbidz-max:assembleRelease :starbidz-max:publishToMavenLocal -x test
